package io.romellpineda.movieapp

object Protocol {

  def browseAll() : String = {
    "running browseAll protocol from Protocol file"
  }
  
  def browseRating() : String = {
    "running browseRating protocol from Protocol file"
  }
  
  def browseTitle() : String = {
    "running browseTitle protocol from Protocol file"
  }
  
  def exit() : String = {
    "running exit protocol from Protocol file"
  }
  
  def pay() : String = {
    "running pay protocol from Protocol file"
  }

  def rent() : String = {
    "running rent protocol from Protocol file"
  }

  def subscribe() : String = {
    "running subscribe protocol from Protocol file"
  }

  def unsubscribe() : String = {
    "running unsubscribe protocol from Protocol file"
  }
}